launch:
  - node: 
      pkg: 'robot_controller'
      exec: 'diff_controller'
      name: 'diff_controller'
      param:
        -
          from: "$(find-pkg-share robot_controller)/config/skid_steer.yaml"
